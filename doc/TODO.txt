- do not try to change things until 6 hours have passed from the last change
- if you do try to change and get the wrong return value then print a message
    that says that it is so (volume is still optimizing or that 6 hours had not
    passwd then work with that accordingly).
- package this module with it's dependencies.
- daemon work
- config file work
    support in pytconf
    document how to configure in the main snipplet
- lvm support
- document how to do manual reducing of volumes in the main README file.
